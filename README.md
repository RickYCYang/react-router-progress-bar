# React Router Progress-bar Example

It's an example of adding a progress bar (like youtube) when toggling route path in a react app.

#### Available

`yarn start` to launch the app in development mode
